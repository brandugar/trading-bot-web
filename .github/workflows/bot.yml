name: Daily Bot Trigger

on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:

jobs:
  ping-render:
    runs-on: ubuntu-latest
    steps:
      - name: Llamar bot en Render
        run: |
          curl --silent "https://trading-bot-web-7ooa.onrender.com/analizar?activos=BTC-USD,ETH-USD,SOL-USD,AVAX-USD,LINK-USD,AAPL,MSFT,GOOGL,AMZN,NVDA,TSLA,QQQ,VTI,ARKK,VT,GLD,SLV,PLTR,SMCI,AMD,MSTR,COIN,MELI,SE"
